# Полное описание функциональности проекта DriveGram

## Общая информация

DriveGram - это приложение облачного хранилища, использующее Telegram в качестве основного хранилища файлов. Проект позволяет пользователям загружать, скачивать и организовывать файлы с использованием API Telegram. Приложение построено на основе Next.js и GramJS.

## Основные возможности

- ✨ Чистый пользовательский интерфейс
- 📁 Организация контента в папках
- 📤 Загрузка/скачивание файлов
- 🔖 Добавление файлов в закладки
- 🗑️ Перемещение файлов в корзину
- 📱 Поддержка нескольких аккаунтов

## Структура проекта

### Главная страница ([app/page.tsx](app/page.tsx:1))

При запуске приложения отображается главная страница. Если у пользователя нет аккаунтов, ему предлагается добавить аккаунт Telegram. После добавления аккаунта пользователь может выбрать один из существующих аккаунтов для работы.

### Страница аккаунта ([app/account/[accountId]/page.tsx](app/account/[accountId]/page.tsx:1))

После выбора аккаунта открывается страница файлового менеджера, содержащая:

- Путь к текущей папке ([Path](lib/components/file-manager/path.tsx:1))
- Панель инструментов ([Toolbar](lib/components/file-manager/toolbar/index.tsx:1))
- Список файлов и папок ([ListItems](lib/components/file-manager/list.tsx:1))

## Интерфейс файлового менеджера

### Путь ([lib/components/file-manager/path.tsx](lib/components/file-manager/path.tsx:1))

Отображает текущий путь к папке и позволяет навигироваться между папками. Пользователь может щелкнуть на любую часть пути, чтобы перейти к этой папке.

### Панель инструментов ([lib/components/file-manager/toolbar/index.tsx](lib/components/file-manager/toolbar/index.tsx:1))

Панель инструментов содержит следующие кнопки:

#### Создание папки ([Folder](lib/components/file-manager/toolbar/create-folder.tsx:1))
- **Кнопка**: "Add Folder" с иконкой папки
- **Функция**: Создает новую папку в текущей директории
- **Доступность**: Всегда доступна

#### Загрузка файлов ([UploadModal](lib/components/file-manager/toolbar/upload-file.tsx:1))
- **Кнопка**: "Upload" с иконкой загрузки
- **Функция**: Открывает модальное окно для загрузки файлов
- **Доступность**: Всегда доступна
- **Особенности**:
  - Поддерживает drag-and-drop файлов
  - Показывает прогресс загрузки для каждого файла
  - Позволяет загружать несколько файлов одновременно
  - Файлы загружаются в текущую папку

#### Добавление в закладки
- **Кнопка**: "Bookmark" с иконкой закладки
- **Функция**: Добавляет выбранный файл в закладки или удаляет из них
- **Доступность**: Доступна только при выборе файла
- **Особенности**: Кнопка меняет текст на "Unbookmark", когда файл уже в закладках

#### Скачивание
- **Кнопка**: "Download" с иконкой скачивания
- **Функция**: Скачивает выбранный файл или папку
- **Доступность**: Доступна при выборе файла или папки

#### Копирование
- **Кнопка**: "Copy" с иконкой копирования
- **Функция**: Создает копию выбранного файла
- **Доступность**: Доступна только при выборе файла
- **Особенности**: Файл копируется в ту же папку с добавлением суффикса "(копия)" к имени

#### Информация
- **Кнопка**: "Info" с иконкой информации
- **Функция**: Отображает информацию о выбранном файле или папке в боковой панели
- **Доступность**: Доступна при выборе файла или папки

#### Перемещение ([MoveFileModal](lib/components/file-manager/move-file-modal.tsx:1))
- **Кнопка**: "Move" с иконкой перемещения
- **Функция**: Перемещает выбранный файл в другую папку
- **Доступность**: Доступна только при выборе файла
- **Особенности**:
  - Открывает модальное окно с деревом папок
  - Позволяет выбрать целевую папку для перемещения
  - После перемещения файл исчезает из текущей папки и появляется в новой

#### Переименование ([RenameModal](lib/components/file-manager/toolbar/rename.tsx:1))
- **Кнопка**: "Rename" с иконкой редактирования
- **Функция**: Переименовывает выбранный файл или папку
- **Доступность**: Доступна при выборе файла или папки
- **Особенности**: Открывает модальное окно с полем для ввода нового имени

#### Удаление
- **Кнопка**: "Delete" с иконкой корзины
- **Функция**: Перемещает выбранный файл или папку в корзину
- **Доступность**: Доступна при выборе файла или папки
- **Особенности**:
  - При удалении файла показывается модальное окно подтверждения с таймером
  - Файл перемещается в корзину, а не удаляется полностью
  - Для папок подтверждение происходит сразу без таймера

## Список файлов и папок ([lib/components/file-manager/list.tsx](lib/components/file-manager/list.tsx:1))

### Отображение файлов ([FileItem](lib/components/file-manager/files/item.tsx:1))

Каждый файл отображается в виде карточки с:

- Иконкой файла (в зависимости от типа файла)
- Именем файла
- Иконкой закладки (заполненная, если файл в закладках)

#### Клик мышью
- **Одиночный клик**: Выделяет файл
- **Правый клик**: Открывает контекстное меню с опциями:
  - Preview (предварительный просмотр)
  - Add to Bookmarks/Remove from Bookmarks (в зависимости от текущего состояния)
  - Copy (копировать файл)
  - Move to Trash (переместить в корзину)
  - File Information (информация о файле)
  - Organize → Move to/Hide
  - Rename (переименовать)
  - Make a copy (создать копию)

#### Двойной клик
- Открывает предварительный просмотр файла ([FilePreview](lib/components/file-manager/file-preview.tsx:1))

#### Иконка закладки
- Клик на иконку закладки переключает статус закладки файла

### Отображение папок ([FolderItem](lib/components/file-manager/folders/item.tsx:1))

Каждая папка отображается в виде карточки с:

- Иконкой папки
- Именем папки

#### Клик мышью
- **Одиночный клик**: Выделяет папку
- **Правый клик**: Открывает контекстное меню с опциями:
  - Open with (открыть с помощью)
  - Download (скачать)
  - Move to Trash (переместить в корзину)
  - File Information (информация о папке)
  - Organize → Move to/Add to Bookmark/Hide
  - Rename (переименовать)
  - Make a copy (создать копию)

#### Двойной клик
- Открывает папку и отображает её содержимое

## Модальные окна

### Подтверждение удаления ([DeleteConfirmation](lib/components/file-manager/delete-confirmation.tsx:1))
- Отображает имя файла, который будет удален
- Показывает таймер обратного отсчета (5 секунд)
- Имеет индикатор прогресса
- Позволяет отменить удаление или подтвердить его

### Перемещение файла ([MoveFileModal](lib/components/file-manager/move-file-modal.tsx:1))
- Показывает путь к текущей папке
- Отображает список доступных папок для перемещения
- Позволяет навигироваться по папкам (вверх/вниз)
- Позволяет выбрать целевую папку для перемещения

### Предварительный просмотр ([FilePreview](lib/components/file-manager/file-preview.tsx:1))
- Поддерживает предварительный просмотр изображений и видео
- Для документов показывает информацию о файле без возможности предварительного просмотра
- Имеет элементы управления для масштабирования и поворота изображений
- Позволяет навигироваться между файлами (если реализовано)

## Добавление аккаунта ([lib/components/accounts/add.tsx](lib/components/accounts/add.tsx:1))

Процесс добавления аккаунта состоит из следующих шагов:

1. **Ввод номера телефона** - пользователь вводит свой номер телефона
2. **Отправка OTP** - приложение отправляет код подтверждения на указанный номер
3. **Ввод OTP** - пользователь вводит полученный код
4. **Двухфакторная аутентификация** (если включена) - ввод пароля от аккаунта
5. **Подтверждение** - после успешной аутентификации отображается приветствие с именем пользователя
6. **Сохранение сессии** - при нажатии "Continue" сессия сохраняется в базе данных

## Выбор аккаунта ([lib/components/accounts/picker.tsx](lib/components/accounts/picker.tsx:1))

Позволяет пользователю выбрать один из добавленных аккаунтов для работы. Также содержит кнопку "Add account" для добавления новых аккаунтов.

## Система обновлений в реальном времени

Проект реализует систему обновлений в реальном времени с использованием WebSocket:

- **WebSocket соединение** ([lib/websocket/client.ts](lib/websocket/client.ts:1)) устанавливается для каждого аккаунта
- **Обработка событий** ([lib/hooks/useRealTimeUpdates.ts](lib/hooks/useRealTimeUpdates.ts:1)) позволяет мгновенно обновлять интерфейс при изменениях
- **Типы событий**: FILE_ADDED, FILE_REMOVED, FILE_UPDATED, FOLDER_CREATED, FOLDER_DELETED, FOLDER_RENAMED
- **Автоматическое восстановление соединения** при разрывах
- **Индикатор состояния подключения** отображается в интерфейсе

## Управление корзиной

- **Перемещение в корзину**: файлы перемещаются в корзину вместо полного удаления
- **Восстановление файлов**: есть возможность восстановить файлы из корзины
- **Окончательное удаление**: файлы могут быть удалены окончательно после определенного периода

## Настройки аккаунта

Доступны на странице [app/account/[accountId]/settings/](app/account/[accountId]/settings/page.tsx) и позволяют управлять параметрами конкретного аккаунта.

## Закладки

- Файлы можно добавлять в закладки для быстрого доступа
- На странице [app/account/[accountId]/bookmark/](app/account/[accountId]/bookmark/page.tsx) отображаются все файлы, добавленные в закладки

## Корзина

- На странице [app/account/[accountId]/trash/](app/account/[accountId]/trash/page.tsx) отображаются файлы, перемещенные в корзину
- Позволяет восстанавливать файлы или удалять их окончательно

## Технические особенности

- **База данных**: используется SQLite для хранения информации о файлах и аккаунтах
- **API**: реализовано с использованием tRPC для типизированных запросов
- **Аутентификация**: через Telegram API с использованием API ID и API Hash
- **Файловое хранилище**: использует встроенные возможности Telegram для хранения файлов
- **Кэширование**: реализовано с использованием TanStack Query (ранее React Query)

## Архитектура

- **Frontend**: Next.js 14 с App Router
- **Backend**: tRPC для API, WebSocket для реального времени
- **UI Components**: Radix UI и собственные компоненты
- **Стилизация**: Tailwind CSS с плагином для анимаций
- **Управление состоянием**: React Context API и tRPC
- **Анимации**: Framer Motion

## Будущие возможности (по дорожной карте)

- Поиск файлов
- Предварительный просмотр файлов
- Интеграция с Google Drive
- Генерация общедоступных ссылок
- Пользовательские чаты для загрузки файлов

Этот проект представляет собой полнофункциональное облачное хранилище, использующее Telegram в качестве основного хранилища файлов, с современным интерфейсом и возможностью работы с несколькими аккаунтами.